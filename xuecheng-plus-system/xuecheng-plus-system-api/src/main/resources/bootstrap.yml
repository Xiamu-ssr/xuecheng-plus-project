server:
  servlet:
    context-path: /system
  port: 63110
#微服务配置
spring:
  application:
    name: system-api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.101.65:3306/xc_system?allowPublicKeyRetrieval=true&serverTimezone=UTC&userUnicode=true&useSSL=false&
    username: root
    password: 1009
# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml
  level:
    org:
      springframework:
        boot: INFO
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
